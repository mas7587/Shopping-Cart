<section>
  <div class="container">
    <div class="row mt-4">
      <!-- this mt -4 is a bootstrap class
                 i will make path for /admin/add-product   this class is bootstrap class ml auto for assign button into right -->
      <a href="/admin/add-product" class="btn btn-success ml-auto">Add Product </a>

    </div>
    <!--mt is margin top  its bootstrap-->
    <table class="table mt-5">
      <thead>
        <tr>
          <th scope="col">No</th>
          <th scope="col">Title</th>
          <th scope="col">Category</th>
          <th scope="col">Description</th>
          <th>Image</th>
        </tr>
      </thead>
      <tbody>
        <!--"{}" this symbol for hbs  -->
        <!--im going to loop 'products array' -->
        {{#each products}}

        <tr>
          <th scope="row">1</th>
          <!-- this.name will take data from user.js -->
          <td>{{this.Name}}</td>
          <td>{{this.Category}}</td>
          <td>{{this.Description}}</td>
          <!--15th step i change src={{this.image}} to src="/product-images/{{this._id}}.jpg"}} now i clear all data from db and i add new standerd data using add product using  localhost:3000/add-product  (16th step in users.js )-->
          <td><img style="width: 50px;height: 50px;" src="/product-images/{{this._id}}.jpg" alt=""></td>
          <!--TODO:50 Delete product Delete button and edit button
          im going to add a delete button in a admin panal-->
          <td>
            <!--TODO:58 Edit product Assaign product id. now we need one route for this edit button-->
             <a href="/admin/edit-product/{{this._id}}" class="btn btn-primary" >Edit</a>

             <!--TODO:52 Delete product; delete the product "id"
             i will give {{this._id}} for delete particular products id
             FIXME1.1 Delete product Another method to delelte object id from database  1.1 <a href="/admin/delete-product?id={{this._id}}&name=mas"class="btn btn-danger">Delete</a>  this "?id" one method-->
                                                                        <!--TODO:56 Delete product confirmation are you want to delete -->
            <a href="/admin/delete-product/{{this._id}}"class="btn btn-danger"onclick="return confirm('Are you want to delete {{this.name}} ?')" >Delete</a>
           
          </td>
        </tr>
        {{/each}}

      </tbody>
    </table>
  </div>
</section>